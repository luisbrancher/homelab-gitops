---
apiVersion: v1
kind: PersistentVolume
metadata:
  name: immich-pv
spec:
  capacity:
    storage: 100Gi
  accessModes:
    - ReadWriteOnce
  storageClassName: ""
  nfs:
    server: 10.10.10.15
    path: /mnt/data-ssd/immich
  persistentVolumeReclaimPolicy: Retain
---
apiVersion: v1
kind: PersistentVolumeClaim
metadata:
  name: immich-pvc
  namespace: immich
spec:
  accessModes:
    - ReadWriteOnce
  storageClassName: ""
  resources:
    requests:
      storage: 100Gi
  volumeName: immich-pv
---
apiVersion: v1
kind: PersistentVolume
metadata:
  name: immich-nextcloud-photos-pv
spec:
  capacity:
    storage: 1500Gi
  accessModes:
    - ReadOnlyMany
  storageClassName: ""
  nfs:
    server: 10.10.10.15         
    path: /mnt/data-ssd/nextcloud
  persistentVolumeReclaimPolicy: Retain
---
apiVersion: v1
kind: PersistentVolumeClaim
metadata:
  name: immich-nextcloud-photos-pvc
  namespace: immich
spec:
  accessModes:
    - ReadOnlyMany
  storageClassName: ""
  resources:
    requests:
      storage: 1500Gi
  volumeName: immich-nextcloud-photos-pv